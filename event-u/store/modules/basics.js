const state = {
    activePage: '',
}

// getters
const getters = {

}

// actions
const actions = {

}

// mutations
const mutations = {
    increment(state) {
        // mutate state
        state.count++
    }
}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}